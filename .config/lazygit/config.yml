git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never

customCommands:
  - key: "C"
    command: "goji"
    context: "files"
    loadingText: "opening commitizen commit tool"
    subprocess: true

gui:
  showCommandLog: false
