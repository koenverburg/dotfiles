- clean: ['~']

- create:
    - ~/.config/alacritty
    - ~/code/tools

- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.tmux.conf: ./ansible/playbooks/roles/tmux/files/.tmux.conf
    ~/.config/nvim: ./.config/nvim
    ~/.config/fish/config.fish: ./ansible/playbooks/roles/fish/files/config.fish
    ~/.config/alacritty/alacritty.yml: ./.config/alacritty/alacritty.yml

- shell:
  # - [git submodule update --init --recursive, Installing submodules]
  - sudo apt-get install git make cmake gcc ninja-build -y # Neovim stuff
  - sudo apt-get install ruby python3 nodejs golang -y # Languages I write in
  # - sudo apt-get install terraform ansible -y # Automation tooling
  - sudo apt-get install fish tmux -y # Shell goodies
  # - make neovim-linux

  #~/AppData/Roaming/Code/User/settings.json: .config/vscode/settings.json
  #~/AppData/Roaming/Code/User/keybindings.json: .config/vscode/keybindings.json

